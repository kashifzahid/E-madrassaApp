<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".TeachersConsole.TeacherClass">

    <include
        layout="@layout/topbar"
        android:id="@+id/teacher_class_toolbar">
    </include>
<EditText
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginLeft="20dp"
    android:layout_marginRight="20dp"
    android:textSize="20sp"
    android:layout_marginTop="10dp"
    android:padding="5dp"
    android:hint="  Search"
    android:drawableLeft="@drawable/ic_search_black_24dp"
    android:id="@+id/searchclass"
    android:background="@drawable/searchclassbox"
    android:layout_below="@id/teacher_class_toolbar"
    android:layout_marginBottom="5dp"
    android:cursorVisible="true"
    android:textColor="#000"
    android:visibility="gone"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/student_preview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="20dp"
        android:layout_below="@id/searchclass"/>

</RelativeLayout>